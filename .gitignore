# Dependencias de Node.js (NO subas esta carpeta)
node_modules/

# Archivos de entorno (variables sensibles como claves API)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Compilaciones (archivos generados al hacer `npm run build`)
dist/
build/

# Archivos de logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos del sistema (Windows y macOS)
.DS_Store
Thumbs.db

# Archivos de lock (según el gestor de paquetes que uses)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Archivos de cache y temporales de npm y Yarn
.cache/
.npm/
.yarn/

# Archivos relacionados con testing
coverage/

# Archivos binarios compilados
*.exe
*.dll
*.so
*.dylib
*.out

# Dependencias descargadas por Go Modules (no es necesario subirlas)
vendor/

# Archivos de compilación
*.o
*.a
*.lo
*.la
*.swp
*.swo

# Archivos de caché y compilación generados por Go
*.test
*.cover
*.prof
*.log
*.tmp

# Archivos del sistema (Windows y macOS)
.DS_Store
Thumbs.db

# Caché de Go Modules (no subir el contenido de pkg/mod)
pkg/mod/

# Archivos de configuración o dependencias locales
.env
.env.local

# Reportes de cobertura de tests
coverage.out

# Si usas JetBrains GoLand o VSCode, puedes ignorar estos archivos
.idea/
.vscode/
